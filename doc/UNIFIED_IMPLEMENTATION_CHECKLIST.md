# Unified Implementation Checklist
## CCS v2 execution tracker (C1-C9)

Source plan: `/Users/vladparakhin/projects/arbiter/doc/UNIFIED_IMPLEMENTATION_PLAN.md`

Last updated: 2026-03-05

---

## 0. Execution Order

Recommended sequence:
`C6 -> C4 -> C5 -> C3 -> C1 -> C2 -> C7 -> C8 -> C9`

---

## 1. Task Tracker

### C6 — Add `planning_canonical.yaml` (🟡)

- [x] Add `benchmarks/scenarios/planning_canonical.yaml`
- [x] Use actual scenario schema (`simulation`, `network`, `scenario`, `artifacts`, `strategies`, `transient`, `context_semantics`)
- [x] Set canonical params (`n=4`, `S=40`, `|d|=4096`, `V=0.05`, low `write_probability`)
- [x] Validate with scenario loader

Acceptance:
- [x] `load_scenario("benchmarks/scenarios/planning_canonical.yaml")` succeeds

### C4 — Add `tools/run_step_scaling.py` (🟡)

- [x] Create tool script with standalone `argparse` entry
- [x] Sweep `duration_ticks` over `[5, 10, 20, 40, 50, 100]`
- [x] Use `run_strategy_comparison` (`eager`, `lazy`)
- [x] Write `benchmarks/results/step_scaling.json`
- [x] Print human-readable sweep rows

Acceptance:
- [x] Output JSON has expected keys per row (`S`, eager/lazy sync means, savings)
- [x] Growth trend: eager approx linear in `S`
- [ ] Growth trend: lazy stays materially flatter than eager in this model (currently not observed)

### C5 — Add `tools/run_artifact_scaling.py` (🟡)

- [x] Create tool script with standalone `argparse` entry
- [x] Sweep artifact sizes `[4096, 8192, 32768, 65536]`
- [x] Use `run_strategy_comparison` (`eager`, `lazy`)
- [x] Write `benchmarks/results/artifact_scaling.json`
- [x] Include `absolute_savings_M` output field

Acceptance:
- [x] Output JSON has expected keys per row
- [x] Savings ratio approximately stable across sizes
- [x] Observed ratio is stable but near `0.0` in current model (lazy not outperforming eager under this parameterization)

### C3 — Verify/freeze baseline in `benchmarks/results/step5/` (🟡)

- [x] Cross-check `benchmarks/results/step5/SUMMARY.md` against paper §8 values
- [x] Confirm expected edge cases (`read_heavy` and `access_pointer` near-zero savings)
- [x] If matched, record confirmation + checksum in `manifest.json`
- [x] If mismatched, document reconciliation path (paper vs scenario params)

Acceptance:
- [x] Baseline/paper consistency statement added to docs or commit log

### C1 — Create `reproduce.sh` (🔴)

- [x] Add root `reproduce.sh` with `set -euo pipefail`
- [x] Install deps: `pip install -e ".[dev]"`
- [x] Run `python tools/run_step5_benchmarks.py`
- [x] Run new sweeps (`run_step_scaling.py`, `run_artifact_scaling.py`)
- [x] Run `python tools/verify_baseline.py ...`
- [x] Ensure outputs remain under `benchmarks/results/`

Acceptance:
- [x] `bash reproduce.sh` exits 0 in a clean Python 3.11 env

### C2 — Write `REPRODUCE.md` (🔴)

- [x] Add root `REPRODUCE.md`
- [x] Document requirements and quick-start clone/run steps
- [x] Add output mapping table using real repo paths
- [x] Document committed baseline location (`benchmarks/results/step5/`)
- [x] Document simulation scope and explicit non-modeled dimensions

Acceptance:
- [x] All documented paths exist or are generated by reproduce workflow

### C7 — Add `src/ccs/core/granularity.py` (🟡)

- [x] Add granularity enum/spec module (`COARSE`, `MEDIUM`, `FINE`)
- [x] Add canonical constants (`CANONICAL_ARTIFACT_TOKENS`, `DEFAULT_GRANULARITY`)
- [x] Export symbols from `src/ccs/core/__init__.py`
- [x] Add `tests/test_granularity.py`

Acceptance:
- [x] New granularity tests pass

### C8 — Rename metric `crr` -> `sync_broadcast_ratio` (🟡)

- [x] Update `src/ccs/simulation/metrics.py` property and docs
- [x] Update `to_dict()` key to `sync_broadcast_ratio`
- [x] Update `src/ccs/simulation/aggregation.py` field names
- [x] Update downstream references (reports/tests/tools/templates)

Acceptance:
- [x] `rg -n '\\.crr\\b|"crr"|crr_mean|crr_std' src tests tools` has no stale metric-name hits
- [x] Test suite remains green

### C9 — Metadata/docs polish (🟡)

- [x] Add root `README.md`
- [x] Add/verify `readme = "README.md"` in `pyproject.toml`
- [x] Add keywords/classifiers/project URLs in `pyproject.toml`
- [x] Keep metadata aligned with repository Apache-2.0 license

Acceptance:
- [x] Packaging metadata is consistent and renderable

---

## 2. Progress Snapshot

- [x] Plan v2 documented and aligned to validated repo structure
- [x] Task sequence and dependency graph defined
- [x] C1-C9 implementation started
- [x] C1-C9 implementation completed

---

## 3. End-to-End Validation (post-implementation)

- [x] `python tools/check_architecture.py`
- [x] `pytest -q`
- [x] `python tools/benchmark_regression_smoke.py`
- [x] `python tools/run_step5_benchmarks.py`
- [x] `python tools/run_step_scaling.py --output benchmarks/results/step_scaling.json`
- [x] `python tools/run_artifact_scaling.py --output benchmarks/results/artifact_scaling.json`
- [x] `bash reproduce.sh`
